// Categories whose activities can be played on the website, for everyone
// (as well as downloaded and printed). Each has its own player on
// /dashboard/play/<activity id>.
export const PLAYABLE_CATEGORIES = new Set(["Sudoku", "Word Searches", "Crosswords"]);
